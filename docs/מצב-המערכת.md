# מצב המערכת — קרא אותי ראשון

מסמך מצב למי שנכנס לפרויקט (אדם או סשן חדש). **המערכת חיה ומשמשת בפועל.**
לא מדריך הפעלה — לזה יש [`docs/הפעלה.md`](הפעלה.md). כאן רק: מה כבר מוגדר,
איפה הדברים, ומה כבר נפל פעם.

עודכן: 22 בספטמבר 2026.

---

## כתובות

| מה | איפה |
|---|---|
| האפליקציה | https://sg437.github.io/mikveh-automation/ |
| אפליקציית הדיונים | https://sg437.github.io/mikveh-automation/talk/ |
| עורך ה-Apps Script | `scriptId` שב-`.clasp.json` |
| הגיליון | לפי `MIKVAOT_SHEET_ID` שבמאפייני הסקריפט |

---

## הכל אוטומטי — אין סבבים ידניים

| שינוי ב | Workflow | מה קורה |
|---|---|---|
| `app/**` | `pages.yml` | פרסום ל-GitHub Pages |
| קבצי `.js` בשורש | `apps-script.yml` | `clasp push` + `clasp deploy` על **אותה פריסה** |

דחיפה ל-`main` מספיקה. **אין צורך ב-`UPLOAD.bat` וב-Deploy ידני** — הם נשארו כגיבוי בלבד.

שני סודות ב-Settings ⟵ Secrets ⟵ Actions: `CLASPRC_JSON`, `CLASP_DEPLOYMENT_ID`.

> ⚠️ **אין להריץ `sync.bat`** בכיוון הזה. הוא מושך מה-Apps Script אל המחשב
> ודוחף ל-GitHub — כלומר ידרוס קוד חדש בגרסה ישנה.

---

## מה כבר מוגדר במאפייני הסקריפט

מוגדר: `MIKVAOT_SHEET_ID` · `MIKVEH_ARCHIVE_FOLDER_ID` · `API_TOKEN` ·
`GOOGLE_CLIENT_ID` · `WA_AUTO_ACTIONS=1` · `NOTIFY_WHATSAPP=1` · `ALERT_CHAT_ID` ·
`GREEN_ID_INSTANCE` · `GREEN_API_TOKEN` · `GROUP_CHAT_ID`

**צריך להיות מוגדר ב-Variables של הריפו** (Settings ⟵ Secrets and variables ⟵
Actions ⟵ Variables): `MIKVEH_API_URL` — כתובת ה-Web App. בלעדיו האתר מתפרסם
בלי חיבור, וכל מי שפותח את הקישור מקבל "המערכת אינה מחוברת" (ולפני התיקון של
ספטמבר 2026 — את כל עותק ההדגמה, בלי שום כניסה).

**לא מוגדר, במכוון:**

- `OPEN_SIGNUP` — בלעדיו הרישום **סגור**: רק מי שמנהל הוסיף במסך "משתמשים" נכנס.
  זו דרישה מפורשת. אין להפעילו בלי בקשה.
- `WA_BRIDGE` — גשר לקבוצת הוואטסאפ. שולח **רק הודעה על תכנון עבודה שנפתח**;
  לא דיווחים, לא דוחות פיקוח ולא דיונים.
- `DEFAULT_ROLE` — חסר משמעות כל עוד הרישום סגור.
- `REQUIRE_LOGIN` — בלעדיו ברירת המחדל היא **כניסה חובה** לכל קריאת נתונים
  (כי `GOOGLE_CLIENT_ID` מוגדר). `REQUIRE_LOGIN=0` יפתח את הנתונים לכל מי שיש
  לו את הכתובת. אין להפעילו.
- `WA_POLL_ANNOUNCE` — בלעדיו כל לקיחה בסקר מוכרזת בקבוצה. `0` מכבה רק את זה;
  הודעת "כבר נלקח" נשלחת תמיד.

מסך **"הגדרות והרשאות" ⟵ "חיבור לגיליון"** מציג טבלת מצב חיה של כל אלה.

---

## פונקציות בדיקה (להרצה מהעורך)

| פונקציה | מה בודקת |
|---|---|
| `testApi` | קריאת הגיליון — מדפיסה ספירות ומקווה לדוגמה |
| `testApiSpeed` | זמן וגודל לכל לשונית. **עוקף את המטמון** |
| `testDataCache` | שהמטמון באמת נתפס |
| `testDataTriggers` | שיש מי שבונה את המטמון ברקע (זו הסיבה הנפוצה לפתיחה איטית) |
| `installDataCacheTrigger` | **מריצים פעם אחת** – מתקין את טריגר הרענון |
| `testWhatsappNotify` | התראות אישיות. כל ההתראות מחריגות את מי שביצע, ולכן אי אפשר לבדוק דרך המסך |

בנוסף, שני קבצי בדיקות מריצים את קוד הצד-שרתי מחוץ לגוגל מול גיליון מדומה
(`tools/gas-stub.js`), בלי לגעת בגיליון האמיתי:
- `node tools/server-test.js` — פרויקטים, הרשאות ותפקידים.
- `node tools/poll-test.js` — סקר חלוקת העבודה מקצה לקצה: פתיחת משימות ➜ שליחת
  הסקר ➜ webhook של סימון ➜ רישום ב"שיבוצים" ובדיון ➜ ההכרזה המיידית ➜ הודעת
  הסיכום (כולל שהיא אינה נשלחת פעמיים).
- `node tools/cache-test.js` — מטמון הנתונים: מי בונה, מתי, וכמה פעמים נקרא הגיליון.
- `node tools/ask-test.js` — שאלה לקבוצה והתשובות: שליחה, תשובה בציטוט, תשובה
  לפי תבנית (מייל), השלמת האימייל בלשונית "משתמשים", וההתראה לשואל.
- `node tools/boot-test.js` — פתיחת האפליקציה בדפדפן אמיתי (Playwright) מול שרת
  מדומה ואיטי בכוונה: שהמסך עולה מהעותק השמור לפני שהשרת עונה, שהנתונים
  החיים מחליפים אותו בלי שגיאות, **ושמי שפותח את הקישור בלי כניסה רואה מסך
  נעילה בלבד** — בלי נתונים, בלי תפריט, ובלי לטעון את `data.js`. בלי
  playwright הבדיקה מדלגת.

---

## ביצועים — מה כבר נעשה

בניית `?action=data` נמשכה 20 שניות בכל רענון. שלושה תיקונים:

1. **מדורי דוחות הפיקוח** אינם נבנים בטעינה הראשית (1.6MB). נטענים לפי דרישה
   ב-`?action=inspections&mikveh=...` כשנפתח כרטיס.
2. **רוחב הקריאה מוגבל** — `apiRows_(ss, name, maxCols)`. `getLastColumn` מחזיר את
   העמודה האחרונה שיש בה תוכן **כלשהו**, ותא בודד רחוק מימין ניפח כל שורה.
3. **מטמון בשרת** (`apiCachedDataJson_`), 10 דקות, בפיסות של 40,000 תווים —
   המגבלה היא 100KB **בבתים**, ואות עברית תופסת שניים.

התוצאה אז: 17 שניות בבנייה, 170ms מהמטמון. (מאז גדלו הנתונים; המדידה
העדכנית למטה.) הודעות ושיבוצים מגיעים ב-`?action=sync` כל 40 שניות.

### ומה שעדיין האט — הסיבה האמיתית ל"לפעמים מהר, לפעמים לאט"

המטמון היה נכון, אבל **כמעט תמיד ריק**, ולכן רוב הפתיחות שילמו את 17 השניות:

1. **הוא חי 10 דקות בלבד.** צוות של כמה מפקחים נכנס כמה פעמים ביום, ולא כל
   עשר דקות. מי שנכנס שעה אחרי הקודם מצא מטמון ריק.
2. **כל כתיבה מחקה אותו לגמרי** — וגם כל דיווח שנקלט מהוואטסאפ
   (`processQueue` רץ כל דקה). המשתמש הבא שילם את הבנייה במלואה.
3. **אף אחד לא בנה מראש.** הבנייה תמיד נפלה על בקשה של משתמש שממתין למסך.

**מה שנעשה** (`Api.js`): הבנייה עברה לרקע, והבקשה מחזירה תמיד את מה ששמור.

| | קודם | עכשיו |
|---|---|---|
| תוקף המטמון | 10 דקות | 6 שעות (המקסימום) |
| אחרי כתיבה | מחיקה; הבא בתור בונה הכול מחדש | סימון בלבד; מוגש העותק הקודם |
| מי בונה | המשתמש שממתין | טריגר ברקע (חד-פעמי 5 שניות אחרי כתיבה, ותקופתי כל 5 דקות) |
| טריות אחרי דיווח | מיד (במחיר הבנייה) | תוך פחות מדקה |
| טריות אחרי עריכה ידנית בגיליון | עד 10 דקות | עד 13 דקות בפעילות, עד 55 בשקט |

הטריגר התקופתי מחזיק את העותק חם **תמיד**, בשתי מדרגות: כל 13 דקות כשמישהו
נכנס לאפליקציה בחצי השעה האחרונה, וכל 55 דקות גם בשקט מוחלט. נפילה ליצירת
טריגר (אין הרשאה) מחזירה אוטומטית להתנהגות הישנה.

> הגרסה הראשונה כאן ויתרה על התחזוקה בשקט, כדי לחסוך מכסת זמן ריצה. זו
> הייתה טעות, ונמדדה בשטח: אצל צוות שנכנס כמה פעמים ביום כמעט כל אחד הוא
> "הראשון אחרי שקט", והוא זה ששילם את הבנייה — **39 שניות** עד שהנתונים
> הופיעו. בנייה אחת לשעה בשקט היא כ-24 ביממה; זול לעומת זה.

### המספרים האמיתיים (נמדד ספטמבר 2026)

`testDataCache`: בנייה **22.9 שניות** · קריאה מהמטמון **308ms** · 2367KB ב-61 פיסות.

`testApiSpeed` — מי אוכל את 23 השניות:

| לשונית | זמן | רשומות | גודל |
|---|---|---|---|
| פעולות | 7.7 ש' | 4,080 | 582KB |
| מקוואות | 6.5 ש' | 1,006 | 948KB |
| דיווחי וואטסאפ | 3.5 ש' | 1,178 | 401KB |
| דוחות פיקוח | 2.5 ש' | 576 | 261KB |
| משימות | 0.8 ש' | 272 | 48KB |
| כל השאר יחד | 2.7 ש' | | 11KB |
| **סך הכל** | **23.7 ש'** | | **2,250KB** |

אלה קריאות `getValues` מול הגיליון, כ-150,000 תאים. זה המחיר של Apps Script,
ואין בו הרבה מה לקצץ. המסקנה אינה "לייעל את הבנייה" אלא **שאיש לא ימתין לה**:
היא רצה ברקע, והבקשה מקבלת עותק מוכן ב-308ms.

**חובה להריץ פעם אחת בעורך: `installDataCacheTrigger`.** בלעדיו אין מי שיבנה
ברקע. `testDataTriggers` אומר אם הוא מותקן, ומסך "חיבור לגיליון" מציג את זה
בשורת **"מטמון הנתונים בשרת"**.

שני קיצורים נוספים במסלול שאמור להיות מיידי:
- `authSessionFast_` — זיהוי המשתמש מהמטמון, בלי `openById` בכל בקשה.
- `getProp_` — כל המאפיינים נקראים פעם אחת להרצה, לא פנייה לשירות לכל מפתח.
- `apiRowsParts_` — רשימת דוחות הפיקוח נקראת בלי 70 עמודות המדורים שבאמצע
  (כ-40,000 תאים שנקראו בכל בנייה ואיש לא הסתכל בהם).

בדיקה: `node tools/cache-test.js` מריץ את כל זה מול קוד השרת האמיתי.

### ואיפה השניות שנשארו — מדידה במקום ניחוש

אחרי שהשרת הפסיק לבנות בכל בקשה, הפתיחה עדיין לוקחת כמה שניות. ארבעה
דברים שונים מסתתרים מאחורי "לוקח זמן לעלות", ולכל אחד תיקון אחר, ולכן
האפליקציה **מודדת את עצמה** בכל פתיחה:

```
⏱️ פתיחת המערכת: סך הכל 0.3 שניות · בניית המסך 106ms
⏱️ הנתונים החיים הוחלפו אחרי 3.6 שניות
```

כשהמסך עולה מהעותק השמור אין בשורה הראשונה "המתנה לשרת" — זה הסימן שהיא
לא חיכתה לגיליון. השורה השנייה אומרת כמה הגיליון באמת לקח.

שתיהן נרשמות ל-console, ואותם מספרים מוצגים במסך **"חיבור לגיליון"** בשורת
**"הפתיחה האחרונה במכשיר הזה"**. איך לקרוא אותם:

| מה גדול | מה זה אומר | לאן ללכת |
|---|---|---|
| המתנה לשרת | הגיליון או הרשת | `testDataTriggers` – המטמון חם? אם כן, זה גודל התשובה או Apps Script עצמו |
| MB | התשובה פשוט גדולה | לצמצם את מה שנשלח בטעינה הראשית |
| פענוח | JSON.parse על מגה-בייטים | כנ"ל – פחות נתונים |
| בניית המסך | הקוד שלנו | הרינדור |
| סך הכל הרבה מעל כולם יחד | הורדת קבצי האפליקציה | ה-Service Worker |

**ה-Service Worker**: קבצי האפליקציה (כ-318KB ב-15 קבצים) נמשכו מהרשת בכל
פתיחה, כי המדיניות הייתה "רשת קודם" לכל בקשה. עכשיו הם **מטמון קודם**
(עם רענון ברקע לפעם הבאה), ורק הנתונים מהגיליון נמשכים מהרשת קודם.
תופעת לוואי: אחרי העלאת גרסה, הרענון הראשון עדיין מציג את הגרסה הקודמת
והשני מציג את החדשה — זו ההתנהגות הרגילה של PWA.

**הפתיחה לא ממתינה לנתונים**: `DataSource.saved()` קורא את התשובה מהפתיחה
הקודמת (מהמטמון של ה-Service Worker) ומצייר אותה מיד, והתשובה החיה מחליפה
אותה כשהיא מגיעה. בכותרת מופיע "עותק שמור · מתעדכן מהגיליון…" עד שזה קורה.
שלוש נקודות שנפלנו בהן ואסור לשכוח:

- `window.MK` חייב להיות מוגדר **לפני** עדכון הנתונים, לא אחרי אתחול
  המסכים: `projects.js` פונה אליו כבר בעדכון המונים.
- `MikvehAuth.init` נקרא שוב כשהתשובה החיה מגיעה. הוא בנוי לכך (סשן שפג
  מתנקה, ומסך הכניסה נפתח, רק כש-`source === 'live'`), ובציור מהעותק השמור
  הוא לא יכול להכריע.
- הציור מחדש מדלג כשטופס פתוח או ששדה בהקלדה — לא מושכים מסך תחת הידיים.

**מטמון הנתונים** יושב תחת שם משלו (`mikveh-data`) ולא נמחק בהעלאת גרסה,
אחרת כל גרסה חדשה הייתה מחזירה את כולם להמתנה מלאה בפתיחה שאחריה.

מוקש ב-Service Worker: `request.destination` של `fetch()` רגיל הוא **מחרוזת
ריקה**, לא `'empty'`. סיווג לפיו שלח את בקשת הנתונים למסלול של קבצי
האפליקציה. הסיווג נעשה לפי רשימת הקבצים (`SHELL`) ו-`mode === 'navigate'`.

### התוצאה (נמדד במחשב, ספטמבר 2026)

| | לפני | אחרי |
|---|---|---|
| עד שיש משהו על המסך | 39 שניות של מסך ריק | **0.3 שניות** |
| עד שהנתונים החיים על המסך | 39.3 שניות | **3.6 שניות** |
| בניית הנתונים בשרת | בבקשה של משתמש | ברקע, 22.9 שניות, אף אחד לא ממתין |
| הגשה מהמטמון | — | 308ms |

3.6 השניות שנשארו הן הורדת 2.25MB ופענוחם, והן אינן חוסמות: המסך כבר עומד.
אם יום אחד זה יפריע — דיווחי הוואטסאפ (401KB, 18% מהחבילה) יכולים להיטען רק
כשנכנסים למסך שלהם, כמו שכבר נעשה למדורי דוחות הפיקוח.

---

## מוקשים שכבר נפלנו בהם

- **`sw.js`** — להעלות את מספר הגרסה **בכל שינוי ב-`app/`**, אחרת משתמשים
  מקבלים קבצים ישנים. `data.js` (3.6MB) אינו ברשימת ההורדה המוקדמת בכוונה.
  `SHELL` מחושב מ-`FILES` ולכן חייב לבוא **אחריו** בקובץ; הצבתו לפניו הפילה
  את רישום ה-Service Worker כולו, בשקט (הדף פשוט עבד בלי מטמון).
- **`caches.match` סורק את *כל* המטמונים של האתר** ומחזיר את ההתאמה הראשונה,
  אלא אם מוסרים לו `{ cacheName }`. שתי האפליקציות חולקות קבצים (`app.js`,
  `perms.js`, `auth.js`...), ואפליקציית הדיונים שומרת אותם במטמון שלה — ולכן
  האפליקציה הראשית הוגשה משם. המטמון של הדיונים מתעדכן רק כשפותחים אותם,
  כך שמי שלא פתח אותם קיבל קבצים שקפאו, ללא הגבלת זמן ובלי קשר לכמה גרסאות
  נדחפו. `index.html` אינו משותף, ולכן העיצוב התעדכן בזמן שהלוגיקה נשארה
  מאחור — וזה הקשה מאוד על האבחון. **כל קריאה למטמון חייבת `{ cacheName }`.**
  (ספטמבר 2026. השחזור: לפתוח `/talk/`, לשנות קובץ משותף, להעלות את גרסת
  ה-SW הראשי בלבד, ולרענן — הגרסה הישנה ממשיכה לרוץ.)
- **`install` חייב `cache: 'reload'`** ב-`addAll`, אחרת הוא מושך דרך מטמון
  ה-HTTP של הדפדפן. GitHub Pages מגיש עם `max-age=600`, ושתי גרסאות בהפרש
  דקות עלולות להכניס קובץ ישן למטמון החדש. מאותה סיבה הרישום נעשה עם
  `updateViaCache: 'none'` — אחרת גם `sw.js` עצמו מוגש ממטמון ה-HTTP.
- **scope של PWA** — האפליקציה הראשית מוגדרת `"scope": "index.html"` ולא `"./"`.
  עם `"./"` היא בולעת את `/talk/`, וכרום מסרב להתקין את הדיונים בנפרד
  ("האפליקציה הזו כבר מותקנת").
- **`Logger.log`** אינו תומך ביישור לרוחב (`%-18s`). יש `pad_()`.
- **פונקציה שמסתיימת בקו תחתון** אינה מופיעה בתפריט ההפעלה של העורך.
- **`writeMessage_`** חייב לקבל `_system: true` להודעות מערכת, אחרת הן נשמרות
  כהודעת משתמש וחוזרות מהגיליון עם "השב" ותגובות אימוג'י.
- **מחזור החיים של מקווה בפרויקט** — פתיחת פרויקט על מקווה קיים מסמנת אותו
  "בשיפוץ" (לא פעיל), ואישור הסיום מחזיר אותו ל"פעיל". מקווה שאינו בבסיס
  הנתונים נוסף רק באישור הסיום, ולא בפתיחה.
- **פעולה חדשה בטבלת ההרשאות** — `permsSheet_` מוסיף לה שורה בלשונית. בלי
  השורה היא הוצגה לפי ברירת המחדל אך `authSetPerms_` לא מצא אותה לעדכון.
- **תפקיד "בלנית" הוסב ל"קבלן"** (ספטמבר 2026). שורות שנשמרו בשמו הישן עדיין
  עובדות: `authRole_` ב-`Auth.js` מתרגם אותן, וכותרת העמודה בלשונית "הרשאות"
  מתעדכנת לבד. השדה "בלנית / אחראי" בכרטיס המקווה הוא דבר אחר ולא נגעו בו.
  בצד הלקוח הנרמול נעשה ב-`session()` שב-`auth.js`, ולא בכל אתר תצוגה בנפרד:
  הסשן נשמר במכשיר ל-90 יום ולכן מחזיק שם תפקיד ישן הרבה אחרי ההסבה, וזה
  השפיע גם על בדיקות ההרשאה ולא רק על מה שמוצג. ב-`perms.js` יש `permOf`,
  שנופל חזרה לשם הישן כשהמפתח הנוכחי חסר — אחרת הרשאה קיימת מוצגת "חסום",
  בלי שום סימן שמשהו לא תואם.
- **ה-Client ID מגיע מהשרת** (`data.googleClientId`). אין להקליד אותו בכל מכשיר —
  זה היה מקור ל-`invalid_client`.
- **הקישור נפתח לכל מי שקיבל אותו** (ספטמבר 2026). `app/config.js` פורסם עם
  `apiUrl` ריק, ולכן האפליקציה עבדה אצל מי שקיבל את הקישור מ-`data.js` — כל
  1,006 המקוואות עם הטלפונים — בלי שום כניסה, ועם מסך ההגדרות פתוח. שלושה
  תיקונים, וכל אחד מהם נחוץ בפני עצמו: (1) `MIKVEH_API_URL` נכנס ל-`config.js`
  בזמן הפרסום; (2) `Api.js` אינו מגיש נתונים בלי סשן (`code: 'login'`);
  (3) `app/gate.js` מציג מסך נעילה ואינו נופל ל-`data.js` — עותק ההדגמה מוגש
  רק למי שכבר נכנס במכשיר הזה (או בפתיחה מקומית לפיתוח).
- **טריגר מבוסס-זמן מעביר אובייקט אירוע כארגומנט הראשון** (ספטמבר 2026) — וזו
  הייתה הסיבה שהסיכום של הסקר חזר לקבוצה **כל חמש דקות**. החתימה הייתה
  `workPollSummary(force)`, הטריגר קרא לה עם `{ triggerUid, authMode, ... }`,
  ולכן כל הרצה נחשבה "שליחה כפויה" וכל בדיקות "מה השתנה מאז" דולגו. עכשיו
  `force` הוא `arg === true` בלבד. **כל פונקציה שמותקנת כטריגר חייבת להיות
  בלי פרמטרים, או לבדוק את הארגומנט במפורש.** (זו הפונקציה היחידה בריפו עם
  פרמטר; נבדק.)
- **סיכום הסקר נשלח פעמיים** (ספטמבר 2026, על אותו גל). גם אחרי התיקון הזה
  נשארו שתי דרכים לשלוח כפול: הטביעה נכתבה **אחרי** השליחה, ולכן הרצה שנפלה
  באמצע שלחה שוב; וכל לקיחה נוספת ייצרה סיכום כמעט זהה. עכשיו הטביעה נכתבת
  לפני השליחה (ומוחזרת אם נכשלה), הפונקציה נעולה ב-`LockService`, והסיכום
  נשלח פעם אחת — עוד סיכום רק כשמשימה משתחררת או אחרי שש שעות.
- **אי אפשר לנעול אפשרות בסקר של וואטסאפ.** אין API לכך, ואי אפשר לערוך סקר
  שנשלח. מה שאפשר — וזה מה שנעשה — הוא להודיע מיד: הכרזה על כל לקיחה, והודעה
  אישית למי שסימן משימה תפוסה ("הסימון שלך לא נרשם") עם רשימת הפנויים.

---

## מה פתוח

| שלב | מה | מצב |
|---|---|---|
| 5 | פרויקטי בנייה ושיפוץ | ✅ נבנה, כולל אישור סיום שמכניס את המקווה לשירות. **טרם נבדק בשטח** |
| 6ג | התראות דחיפה (Web Push) במקום גשר הוואטסאפ | ⬜ |
| 7 | הפסקת הוואטסאפ בהדרגה | ⬜ |
| — | חיבור לאתר (`?action=public` + `web/mikvaot-widget.js`) | מוכן בקוד, לא חובר |
| — | הוספת הצוות במסך "משתמשים" | ⬜ — אפשר לאסוף את כתובות המייל בשאלה לקבוצה (הגדרות ➜ שאלה לקבוצת הוואטסאפ) |

---

## לפני שמתחילים לעבוד

> **לא לעבוד בשני סשנים במקביל על הריפו הזה.** כבר הייתה התנגשות בגרסת
> ה-Service Worker כששני סשנים דחפו ל-`main` באותו זמן.
