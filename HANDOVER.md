# מסמך העברה (Handover) — מערכת כשרות המקוואות V2

> מסמך זה נועד למי שמקבל את המערכת לידיו: מה היא עושה, איך היא בנויה,
> אילו חשבונות והגדרות היא דורשת, ומה חייב לעבור בעלות בתהליך ההעברה.
>
> מנהל המערכת היוצא: שמוליק גולדמן

---

## 1. מה המערכת עושה — במשפט אחד

מפקחי כשרות שולחים דיווחים (טקסט / תמונות / הודעות קוליות) בקבוצת וואטסאפ;
המערכת קולטת אותם אוטומטית, מארכבת כל קובץ בדרייב, מנתחת כל דיווח עם Gemini
(זיהוי מקווה, סוג ליקוי, תמלול אודיו), רושמת הכל ביומן מסודר ב-Google Sheets,
מתייקת מדיה בתיקיות לפי מקווה — ושולחת התראות שגיאה ודוח שבועי בוואטסאפ.

## 2. ארכיטקטורה — זרימת המידע

```
קבוצת וואטסאפ
    │  (הודעה נכנסת)
    ▼
Green API  ──POST Webhook──►  Apps Script Web App (doPost בקובץ קוד.js)
    │                              │
    │                              ├─ אימות טוקן (?token=...)
    │                              ├─ העברת עותק ל-Make (תקופת מעבר בלבד)
    │                              └─ רישום שורה בלשונית "תור נכנס", סטטוס "ממתין"
    ▼
טריגר כל דקה: processQueue()
    │
    ├─ שלב 2: הורדת המדיה מ-Green API → העלאה ל"ארכיון הגולמי" (תיקיית MM-YYYY)
    ├─ שלב 3: ניתוח בג'מיני (JSON מובנה) → שורה ב"יומן דיווחים"
    ├─ תיוק חכם: העתקת הקובץ לתיקיית המקווה/היישוב המתאימה
    └─ עדכון סטטוס השורה בתור: "טופל" / "שגיאה"

טריגר כל 5 דקות: checkErrors()  → התראת וואטסאפ על שגיאות חדשות (Alerts.js)
טריגר יום ראשון 08:00: weeklyReport() → דוח שבועי בוואטסאפ (Reports.js)
```

## 3. קבצי הפרויקט

| קובץ | תפקיד |
|---|---|
| `קוד.js` | הליבה: קליטת Webhook (שלב 1), ארכוב גולמי (שלב 2), ניתוח ג'מיני + תיוק חכם + יומן (שלב 3) |
| `Alerts.js` | שלב 4א: שומר שגיאות — התראות וואטסאפ כל 5 דקות, עם מצערת (מקס' התראה אחת ל-10 דק') |
| `Reports.js` | דוח שבועי מרוכז בוואטסאפ — כל יום ראשון ~08:00 |
| `appsscript.json` | הגדרות הפרויקט: אזור זמן ירושלים, Web App פתוח לגישה אנונימית (חובה בשביל ה-Webhook) |
| `.clasp.json` | חיבור clasp לפרויקט ה-Apps Script (scriptId בפנים) |
| `sync.bat` | סקריפט Windows: `clasp pull` → commit → push ל-GitHub (גיבוי הקוד) |

## 4. לשוניות הגיליון (Google Sheets)

המערכת רצה כסקריפט **מוצמד לגיליון** (`SpreadsheetApp.getActiveSpreadsheet`).

| לשונית | תפקיד |
|---|---|
| **תור נכנס** | כל הודעה נכנסת = שורה. עמודה B = `idMessage` (מפתח ייחודי למניעת כפילויות — "פתרון הברזל"). עמודה J = סטטוס: ממתין / טופל / שגיאה |
| **יומן דיווחים** | התוצר המרכזי: דיווח מנותח לכל שורה — מקווה, יישוב, סוג ליקוי, תקציר, תמלול, קישורי מדיה, סטטוס זיהוי |
| **רשימת מיקומים** | הרשימה הקנונית הסגורה: עמודה A = שם מקווה מלא (= שם התיקייה בדרייב), עמודה B = עיר. ג'מיני רשאי להחזיר רק שמות מכאן |
| **שגיאות** | לוג שגיאות — ממנו נשלחות ההתראות |

> ⚠️ **סדר העמודות בלשוניות קבוע בקוד** (`QUEUE_HEADERS`, `REPORT_HEADERS`).
> אין להוסיף/להזיז עמודות בלי לעדכן את הקוד.

## 5. Script Properties — כל המפתחות

(עורך Apps Script → ⚙️ Project Settings → Script Properties)

| מפתח | תפקיד | חובה? |
|---|---|---|
| `WEBHOOK_TOKEN` | טוקן סודי לאימות ה-Webhook (`?token=...` בכתובת ב-Green API). אם ריק — אין אימות | מומלץ מאוד |
| `GROUP_CHAT_ID` | ה-chatId של קבוצת הוואטסאפ (`...@g.us`). אם ריק — נקלט הכל (שימושי לגילוי ה-ID בהתחלה) | כן |
| `RAW_ARCHIVE_FOLDER_ID` | תיקיית הדרייב של "הארכיון הגולמי" | כן (שלב 2) |
| `GEMINI_API_KEY` | מפתח API של Gemini (מ-Google AI Studio) | כן (שלב 3) |
| `GEMINI_MODEL` | דגם ג'מיני. ברירת מחדל: `gemini-2.5-flash` | לא |
| `MIKVEH_ARCHIVE_FOLDER_ID` | תיקיית "ארכיון לפי מקווה" (תיוק חכם) | כן (שלב 3) |
| `LOCATIONS_FOLDER_ID` | תיקיית "מיקומים כלליים" (מדיה לא מזוהה) | כן (שלב 3) |
| `GREEN_ID_INSTANCE` | מספר האינסטנס מ-console.green-api.com | כן (התראות ודוחות) |
| `GREEN_API_TOKEN` | ה-API Token של האינסטנס | כן (התראות ודוחות) |
| `ALERT_CHAT_ID` | למי נשלחות ההתראות והדוח (`9725XXXXXXXX@c.us`) | כן (התראות ודוחות) |
| `MAKE_FORWARD_URL` | Webhook של Make לתקופת המעבר. **מחיקת המפתח = הפסקת ההעברה** | זמני |
| `ALERT_LAST_ROW` | סמן פנימי של שומר ההתראות — לא לגעת ידנית | פנימי |

## 6. טריגרים מותקנים

| פונקציה | תדירות | מתקינים עם |
|---|---|---|
| `processQueue` | כל דקה | `installTrigger()` |
| `checkErrors` | כל 5 דקות | `installAlertTrigger()` |
| `weeklyReport` | יום ראשון ~08:00 | `installWeeklyTrigger()` |

פונקציות ההתקנה בטוחות להרצה חוזרת — הן מוחקות את הטריגר הקיים ומתקינות מחדש.

## 7. חשבונות ונכסים שחייבים לעבור בהעברה

זו ליבת ההעברה — לכל פריט צריך להחליט: העברת בעלות, שיתוף, או החלפת מפתח.

1. **חשבון Google שמחזיק את הכל** — הגיליון, תיקיות הדרייב, פרויקט ה-Apps Script
   והטריגרים רצים בהרשאות הבעלים. העברת בעלות על הגיליון + התיקיות, או מעבר
   לחשבון ארגוני. **שימו לב: טריגרים ופריסת Web App הם אישיים** — הבעלים החדש
   חייב לפרוס Web App משלו ולהריץ את פונקציות ההתקנה מחדש (ראו סעיף 9).
2. **Green API** (console.green-api.com) — החשבון, האינסטנס, והטלפון/מספר
   שמחובר לוואטסאפ. כולל חיוב/מנוי אם קיים.
3. **מפתח Gemini** (aistudio.google.com) — מומלץ שהבעלים החדש ינפיק מפתח משלו
   ויחליף את `GEMINI_API_KEY`.
4. **קבוצת הוואטסאפ** — ניהול הקבוצה עצמה; המספר של האינסטנס חייב להישאר חבר בה.
5. **ריפו GitHub** (`sg437/mikveh-automation`) — גיבוי הקוד. העברת בעלות או הוספת הבעלים החדש.
6. **Make** — אם תקופת המעבר הסתיימה: למחוק את `MAKE_FORWARD_URL` ולסגור את התרחיש הישן.
7. **מחשב הסנכרון** — `sync.bat` רץ מ-`C:\Projects\mikveh-automation` עם clasp
   מחובר (`clasp login`). הבעלים החדש צריך להתקין clasp ולהתחבר בעצמו.

## 8. צ'קליסט העברה מסודר

- [ ] שיתוף/העברת בעלות: הגיליון + 3 תיקיות הדרייב (גולמי, לפי מקווה, מיקומים כלליים)
- [ ] גישה לפרויקט ה-Apps Script (או העתקתו לחשבון החדש)
- [ ] גישה לחשבון Green API + עדכון פרטי חיוב
- [ ] הנפקת `GEMINI_API_KEY` חדש על שם הבעלים החדש
- [ ] העתקת כל ה-Script Properties (סעיף 5) — הם **לא** עוברים עם העתקת קוד!
- [ ] אם הוחלף חשבון: פריסת Web App חדשה (Execute as me + Anyone anonymous)
- [ ] עדכון כתובת ה-Webhook (כולל `?token=...`) בהגדרות האינסטנס ב-Green API
- [ ] הרצת פונקציות ההתקנה מחדש: `installTrigger`, `installAlertTrigger`, `installWeeklyTrigger`
- [ ] עדכון `ALERT_CHAT_ID` למספר של האחראי החדש
- [ ] בדיקות קצה-לקצה (סעיף 10)
- [ ] גישה לריפו GitHub + הגדרת clasp במחשב החדש
- [ ] החלפת `WEBHOOK_TOKEN` (מומלץ אחרי כל העברה)
- [ ] סיום תקופת המעבר: מחיקת `MAKE_FORWARD_URL` וכיבוי התרחיש ב-Make

## 9. הקמה מאפס בחשבון חדש (אם מעבירים חשבון)

1. להעתיק את הגיליון (או לשתף) ולפתוח בו Extensions → Apps Script.
2. להדביק את שלושת הקבצים (`קוד.js`, `Alerts.js`, `Reports.js`) ואת `appsscript.json`.
3. להריץ פעם אחת: `setupSheets()` → `upgradeSheets()` → `setupStage3()`
   (מאשרים את בקשות ההרשאה שקופצות).
4. להדביק את רשימת המקוואות בלשונית "רשימת מיקומים" (עמודה A שם מלא, עמודה B עיר).
5. להזין את כל ה-Script Properties (סעיף 5).
6. לפרוס: Deploy → New deployment → Web app → Execute as **Me**,
   Who has access: **Anyone** (anonymous). להעתיק את כתובת ה-`/exec`.
7. ב-Green API: להגדיר את הכתובת (עם `?token=`) כ-Webhook URL של האינסטנס,
   ולוודא שמופעל `incomingMessageReceived`.
8. להריץ: `installTrigger()`, `installAlertTrigger()`, `installWeeklyTrigger()`.

## 9א. מדריך Green API למנהל הנכנס — צעד אחר צעד

### א. השתלטות על החשבון
1. כניסה ל-console.green-api.com עם הפרטים מהמנהל היוצא.
2. החלפת סיסמה מיד, ועדכון מייל החשבון למייל של המנהל החדש.
3. בדיקת חיוב/מנוי (Billing) — העברת אמצעי התשלום, אחרת האינסטנס ייחסם.

### ב. זיהוי האינסטנס ואימות מול הסקריפט
1. במסוף: לרשום את **idInstance** ואת **apiTokenInstance** של האינסטנס.
2. לוודא שהם זהים ל-`GREEN_ID_INSTANCE` ו-`GREEN_API_TOKEN` ב-Script Properties.
3. מומלץ אחרי העברה: לחדש את הטוקן במסוף (Regenerate) — **ומיד** לעדכן את
   `GREEN_API_TOKEN` בסקריפט. טוקן שחודש בלי עדכון = התראות ודוחות מתים בשקט.

### ג. הגדרות ה-Webhook
1. **Webhook URL** בהגדרות האינסטנס חייב להצביע על כתובת ה-Web App:
   `https://script.google.com/macros/s/XXXXX/exec?token=<WEBHOOK_TOKEN>`
   — הטוקן בכתובת חייב להיות זהה ל-`WEBHOOK_TOKEN` ב-Script Properties,
   אחרת כל ההודעות נדחות בשקט.
2. להפעיל: **Incoming webhooks** (`incomingMessageReceived`) — חובה.
3. אפשר לכבות את כל שאר סוגי ההתראות (יוצאות, סטטוסים) — הקוד ממילא מסנן.
4. שינויים נכנסים לתוקף תוך עד ~5 דקות.

### ד. הפניית ההתראות למנהל החדש
לעדכן ב-Script Properties את `ALERT_CHAT_ID` בפורמט `9725XXXXXXXX@c.us`.

### ה. בדיקות (בסדר הזה)
1. סטטוס האינסטנס במסוף = authorized (אם לא — סריקת QR מחדש).
2. פתיחת כתובת ה-`/exec` בדפדפן → "הקולט פעיל".
3. הרצת `testAlert()` → הודעה מגיעה לוואטסאפ של המנהל.
4. הודעת ניסיון בקבוצה → שורה ב"תור נכנס" → "טופל" + שורה ביומן תוך ~דקה.
   אם רק בדיקה 4 נכשלת — כמעט תמיד Webhook URL שגוי או טוקן לא תואם.

### מלכודות נפוצות
- Deploy **חדש** ב-Apps Script משנה את כתובת ה-`/exec` → חובה לעדכן ב-Green API.
  עדכון גרסה ל-deployment קיים (Manage deployments → Edit → New version) שומר על הכתובת — עדיף.
- המספר המחובר לאינסטנס חייב להישאר חבר בקבוצת הדיווחים.
- לא למחוק את `GROUP_CHAT_ID` — בלעדיו נקלטות הודעות מכל הצ'אטים.

## 10. בדיקות קבלה (Acceptance)

| בדיקה | איך | תוצאה צפויה |
|---|---|---|
| Web App חי | פתיחת כתובת ה-`/exec` בדפדפן | "✅ ... הקולט פעיל" + שעה |
| קליטה | הרצת `testFakeMessage()` | שורה חדשה ב"תור נכנס" בסטטוס "ממתין" |
| ניתוח | להמתין ~דקה (או להריץ `processQueue()` ידנית) | השורה עוברת ל"טופל" + שורה ב"יומן דיווחים" |
| ג'מיני | הרצת `testGemini()` | JSON תקין בלוג, עם זיהוי המקווה |
| התראות | הרצת `testAlert()` | הודעת וואטסאפ מגיעה ל-`ALERT_CHAT_ID` |
| דוח שבועי | הרצת `testWeeklyReport()` | הדוח מגיע בוואטסאפ |
| קצה-לקצה אמיתי | שליחת הודעה + תמונה בקבוצה | תור → יומן → קובץ מתויק בדרייב |

## 11. התנהגויות חשובות שכדאי להכיר (כדי לא "לתקן" אותן בטעות)

- **תמיד מחזירים 200 ל-Green API** — גם על שגיאה. אחרת Green API נכנס ל-Retry אינסופי.
- **מניעת כפילויות כפולה**: מטמון 6 שעות + חיפוש `idMessage` בעמודה B. בנוסף
  `LockService` נגד שני Webhooks באותה שנייה (הפתרון לבאג ה-Race Condition הישן).
- **מכסת עיבוד**: עד 8 שורות לריצה + עצירה אחרי 4.5 דקות (תקרת 6 הדקות של Apps Script).
- **"חדר ההמתנה"**: תמונה שמגיעה בלי טקסט מחכה עד 5 דקות לטקסט משלים מאותו שולח;
  אם תוך חלון של 6 דקות יש דיווח קודם של אותו מפקח — המדיה מוצמדת אליו ביומן.
  אחרת היא מתויקת כ"מדיה יתומה" במיקומים כלליים.
- **רשימה סגורה**: שם מקווה שג'מיני מחזיר ואינו ברשימת המיקומים — יורד לרמת "יישוב".
  עיר עם כמה מקוואות בלי זיהוי חד-משמעי מסומנת "דרוש בירור" (לא מנחשים!).
- **הארכוב הגולמי קודם לכל** — גם אם ג'מיני נופל, הקובץ כבר שמור. ריטריי לג'מיני:
  3 ניסיונות עם המתנה של 15/30 שניות על 429/5xx.
- **רשימת המיקומים במטמון 10 דקות** — שינוי ברשימה נקלט תוך עד 10 דקות.
- **שינוי שם לשונית = שבירת המערכת** — השמות קבועים ב-`CONFIG`.

## 12. נקודות לתשומת לב (חובות טכניים)

- `sync.bat` מכיל `set NODE_TLS_REJECT_UNAUTHORIZED=0` — עוקף אימות TLS של clasp.
  ככל הנראה נוסף בגלל פרוקסי ארגוני; במחשב חדש עדיף להסיר את השורה ולוודא שזה עובד בלעדיה.
- ה-Web App פתוח ל-`ANYONE_ANONYMOUS` (הכרחי ל-Webhook) — לכן חשוב ש-`WEBHOOK_TOKEN`
  יהיה מוגדר תמיד, ולהחליפו בכל העברה.
- מפתחות (Gemini, Green API) שמורים רק ב-Script Properties — אין להם גיבוי.
  לתעד אותם במקום מאובטח (מנהל סיסמאות), לא בקוד ולא בגיליון.
- עמודה M בתור ("JSON גולמי") נחתכת ל-45,000 תווים — הודעות ענק יאבדו זנב (לדיבוג בלבד).

---

*נוצר כחלק מתהליך ההעברה, יולי 2026.*
